name: API CI/CD

on:
  push:
    branches: [main]
    paths:
      - "**/*.py"
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Linting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - run: pip install ruff
      - run: ruff check ./API
  unit-tests:
    name: Running unit tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - run: cd API
      - run: pip install -r requirements.txt
      - run: cd tests && pytest .
  unit-tests-coverage:
    name: Checking unit tests coverage
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - run: cd API
      - run: pip install -r requirements.txt
      - run: pytest --cov=API/tests --cov-fail-under=70
  integration-tests:
    name: Running integration tests
    runs-on: ubuntu-latest
    needs: unit-tests-coverage
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Go into API dir
        run: cd API
      - name: Install requirements
        run: pip install -r requirements.txt
      - name: Create test environment with docker compose
        run: docker compose -f docker-compose.test.yml up -d --build
      - name: Check deployment health
        run: |
          for i in {1..30}; do
            if curl -sf http://localhost:8000/health; then
              exit 0
            fi
            sleep 2
          done
          exit 1
      - name: Start testing
        run: pytest API/tests/integration
      - name: Remove environment
        run: docker compose -f docker-compose.test.yml down -v
